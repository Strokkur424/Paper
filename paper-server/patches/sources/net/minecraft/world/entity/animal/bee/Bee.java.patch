From f632b9eb8d5c757eaffb596f5fe062258ab24e42 Mon Sep 17 00:00:00 2001
From: File <noreply+automated@papermc.io>
Date: Sun, 20 Apr 1997 15:37:42 +0200
Subject: [PATCH] paper File Patches


diff --git a/net/minecraft/world/entity/animal/bee/Bee.java b/net/minecraft/world/entity/animal/bee/Bee.java
index 927d4a7dd68ef45738baa70b3ebe5310fd1788df..74ccf0947edbbdfd42986519f5065b7e4e251d13 100644
--- a/net/minecraft/world/entity/animal/bee/Bee.java
+++ b/net/minecraft/world/entity/animal/bee/Bee.java
@@ -148,10 +148,26 @@ public class Bee extends Animal implements NeutralMob {
     private Bee.BeeGoToHiveGoal goToHiveGoal;
     private Bee.BeeGoToKnownFlowerGoal goToKnownFlowerGoal;
     private int underWaterTicks;
+    public net.kyori.adventure.util.TriState rollingOverride = net.kyori.adventure.util.TriState.NOT_SET; // Paper - Rolling override
 
     public Bee(final EntityType<? extends Bee> type, final Level level) {
         super(type, level);
-        this.moveControl = new FlyingMoveControl<>(this, 20, true);
+        // Paper start - Fix MC-167279
+        class BeeFlyingMoveControl extends FlyingMoveControl<Mob> {
+            public BeeFlyingMoveControl(final Mob mob, final int maxTurn, final boolean hoversInPlace) {
+                super(mob, maxTurn, hoversInPlace);
+            }
+
+            @Override
+            public void tick() {
+                if (this.mob.getY() <= Bee.this.level().getMinY()) {
+                    this.mob.setNoGravity(false);
+                }
+                super.tick();
+            }
+        }
+        this.moveControl = new BeeFlyingMoveControl(this, 20, true);
+        // Paper end - Fix MC-167279
         this.lookControl = new Bee.BeeLookControl(this);
         this.setPathfindingMalus(PathType.FIRE, -1.0F);
         this.setPathfindingMalus(PathType.WATER, -1.0F);
@@ -198,9 +214,18 @@ public class Bee extends Animal implements NeutralMob {
 
     @Override
     protected void addAdditionalSaveData(final ValueOutput output) {
+        // CraftBukkit start - selectively save data
+        this.addAdditionalSaveData(output, true);
+    }
+
+    @Override
+    public void addAdditionalSaveData(final ValueOutput output, final boolean saveAll) {
+        // CraftBukkit end
         super.addAdditionalSaveData(output);
+        if (saveAll) { // Paper
         output.storeNullable("hive_pos", BlockPos.CODEC, this.hivePos);
         output.storeNullable("flower_pos", BlockPos.CODEC, this.savedFlowerPos);
+        } // Paper
         output.putBoolean("HasNectar", this.hasNectar());
         output.putBoolean("HasStung", this.hasStung());
         output.putInt("TicksSincePollination", this.ticksWithoutNectarSinceExitingHive);
@@ -238,7 +263,7 @@ public class Bee extends Animal implements NeutralMob {
                 }
 
                 if (poisonTime > 0) {
-                    livingTarget.addEffect(new MobEffectInstance(MobEffects.POISON, poisonTime * 20, 0), this);
+                    livingTarget.addEffect(new MobEffectInstance(MobEffects.POISON, poisonTime * 20, 0), this, org.bukkit.event.entity.EntityPotionEffectEvent.Cause.ATTACK); // CraftBukkit
                 }
             }
 
@@ -475,7 +500,11 @@ public class Bee extends Animal implements NeutralMob {
         if (this.hivePos == null) {
             return null;
         } else {
-            return this.isTooFarAway(this.hivePos) ? null : this.level().getBlockEntity(this.hivePos, BlockEntityTypes.BEEHIVE).orElse(null);
+            // Paper start - move over logic to accommodate isTooFarAway with chunk load check
+            return this.isTooFarAway(this.hivePos) || this.level().getChunkIfLoadedImmediately(this.hivePos.getX() >> 4, this.hivePos.getZ() >> 4) == null
+                ? null
+                : this.level().getBlockEntity(this.hivePos, BlockEntityTypes.BEEHIVE).orElse(null);
+            // Paper end
         }
     }
 
@@ -507,7 +536,8 @@ public class Bee extends Animal implements NeutralMob {
         return this.getFlag(FLAG_ROLL);
     }
 
-    public void setRolling(final boolean rolling) {
+    public void setRolling(boolean rolling) { // Paper - Rolling override
+        rolling = this.rollingOverride.toBooleanOrElse(rolling); // Paper - Rolling override
         this.setFlag(FLAG_ROLL, rolling);
     }
 
@@ -564,7 +594,7 @@ public class Bee extends Animal implements NeutralMob {
             if (effect != null) {
                 this.usePlayerItem(player, hand, heldItem);
                 if (!this.level().isClientSide()) {
-                    this.addEffect(effect);
+                    this.addEffect(effect, org.bukkit.event.entity.EntityPotionEffectEvent.Cause.FOOD); // Paper - Add missing effect cause
                 }
 
                 return InteractionResult.SUCCESS;
@@ -637,8 +667,9 @@ public class Bee extends Animal implements NeutralMob {
             return false;
         }
 
+        if (!super.hurtServer(level, source, damage)) return false; // CraftBukkit - Only stop pollinating if entity was damaged
         this.beePollinateGoal.stopPollinating();
-        return super.hurtServer(level, source, damage);
+        return true; // CraftBukkit - Only stop pollinating if entity was damaged
     }
 
     @Override
@@ -978,7 +1009,7 @@ public class Bee extends Animal implements NeutralMob {
                             }
                         }
 
-                        if (growState != null) {
+                        if (growState != null && org.bukkit.craftbukkit.event.CraftEventFactory.callEntityChangeBlockEvent(Bee.this, belowPos, growState)) { // CraftBukkit
                             Bee.this.level().levelEvent(LevelEvent.PARTICLES_BEE_GROWTH, belowPos, 15);
                             Bee.this.level().setBlockAndUpdate(belowPos, growState);
                             Bee.this.incrementNumCropsGrownSincePollination();
@@ -1002,7 +1033,7 @@ public class Bee extends Animal implements NeutralMob {
         @Override
         protected void alertOther(final Mob other, final LivingEntity hurtByMob) {
             if (other instanceof Bee && this.mob.hasLineOfSight(hurtByMob)) {
-                other.setTarget(hurtByMob);
+                other.setTarget(hurtByMob, org.bukkit.event.entity.EntityTargetEvent.TargetReason.TARGET_ATTACKED_ENTITY); // CraftBukkit - reason
             }
         }
     }
@@ -1159,7 +1190,7 @@ public class Bee extends Animal implements NeutralMob {
                     Bee.this.dropFlower();
                     this.pollinating = false;
                     Bee.this.remainingCooldownBeforeLocatingNewFlower = 200;
-                } else {
+                } else if (Bee.this.savedFlowerPos != null) { // Paper - add null check since API can manipulate this
                     Vec3 flowerPos = Vec3.atBottomCenterOf(Bee.this.savedFlowerPos).add(0.0, 0.6F, 0.0);
                     if (flowerPos.distanceTo(Bee.this.position()) > 1.0) {
                         this.hoverPos = flowerPos;
