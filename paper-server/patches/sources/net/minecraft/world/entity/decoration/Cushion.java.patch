--- a/net/minecraft/world/entity/decoration/Cushion.java
+++ b/net/minecraft/world/entity/decoration/Cushion.java
@@ -80,11 +_,12 @@
     }
 
     @Override
-    protected void removePassenger(final Entity passenger) {
-        super.removePassenger(passenger);
+    protected boolean removePassenger(final Entity passenger) {
+        boolean removed = super.removePassenger(passenger);
         if (!this.level().isClientSide() && this.getRemovalReason() == null) {
             this.playSound(SoundEvents.CUSHION_GET_UP, 1.0F, 1.0F);
         }
+        return removed;
     }
 
     @Override
